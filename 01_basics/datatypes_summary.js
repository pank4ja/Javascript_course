// primitive 
// number

const score =33;
const scorevalue =100.2;

const islogin =false;
const outtemp= null; // empty hai
let useremail;  // undefined
let valeu =undefined // can also be defined like this


const id = Symbol('123')
const anotherid = Symbol('123')

console.log(id === anotherid);  // symbol in used to define unique item so they are not same even if values are same

const bignumber = 555512454612153424242n; //last me n laga do


// non primitive
const hero = ['sakhti', 'bhuku','oposk'];
let myobj = {
    name : "hitesh",
    age : 22,
}

const myfunction= function ()
{
    log.console("hello from my fun")

}


console.log(typeof bignumber);
console.log(typeof outtemp);
console.log(typeof hero);
console.log(typeof myfunction);

